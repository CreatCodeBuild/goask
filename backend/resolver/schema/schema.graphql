schema {
    query: Query
    mutation: Mutation
}

type Query {
    action(userID: Int!): QueryAction!
}

type Mutation {
    questionMutation(userID: Int!): QuestionMutation!
    answer(userID: Int!): AnswerMutation!
    user: UserMutation!
}
